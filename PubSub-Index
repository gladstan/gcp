Pubsub:

Essentials:
	1. Create a topic, subsciption using gcloud command
	2. Send message and receive using subscription using cli
	3. Ack/unack messages in pubsub, check if unacked messages are replayed
	4. set message expiry and test it
	5. push and pull samples using Cloud Function consumer

Proficient:
	6. Push avro message type with schema in pubsub
	7. Explore dead letter queue when failure happens
	8. 
  
Skilled:
